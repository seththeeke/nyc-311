/*
 * The one content wrapper every page in the primary workspace uses, so the
 * header and content keep the same width and padding as you click between
 * pages — fills the workspace rather than centering in a max-width column.
 */
export const PAGE_CONTENT_CLASSES = "relative w-full px-6 py-10";
